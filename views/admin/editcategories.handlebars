{{#each errors}}
<div class="alert alert-danger">
	{{text}}
</div>
{{else}}
{{/each}}
<h3>Edição de Categoria</h3>
<div class="card">
	<div class="card-body">
		<form action="new" method="POST">
			<label for="name">Nome:</label>
			<input type="name" id="name" name="name" class="form-control" placeholder="Noma da categoria">
			<label for="slug">Slug:</label>
			<input type="name" id="slug" name="slug" class="form-control" placeholder="Slug da categoria">
			</br>
			<button type="submit" class="btn btn-success">Editar</button>
		</form>
	</div>
</div>
